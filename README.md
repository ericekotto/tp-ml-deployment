# TP Machine Learning - Classification & Régression

Ce projet contient 3 modèles de Machine Learning déployés avec Streamlit.

## 📁 Structure du projet
- `app.py` : Application principale Streamlit.
- `models/` : Contient les modèles entraînés (`.pkl`).
- `data/` : Contient les datasets originaux.
- `requirements.txt` : Liste des dépendances.

## 🚀 Installation et Lancement
1. Installer les dépendances : `pip install -r requirements.txt`
2. Lancer l'app : `streamlit run app.py`